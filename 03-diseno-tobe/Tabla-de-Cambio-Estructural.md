# Actividad 3.2 — Tabla de cambio estructural

Inventario completo del TO-BE frente a la línea base del Reto 1
(`Reto_Hacienda_Modernizacion_SOLID/03-src/Modificado/`), obtenido comparando los dos árboles
de código, no de memoria: **1 elemento sale, 17 entran y 23 se transforman**. Las filas agrupan
elementos solo cuando el cambio es literalmente el mismo en todos ellos; la columna «Elemento»
dice cuántos son en ese caso.

Se cita el archivo tal como está en `src/Modificado/`. `RaizComposicion.cs` aparece una sola vez
(E-32) porque los cuatro patrones se ensamblan ahí: es el archivo que concentra el cambio.

## Lo que sale

| ID | Elemento | Estado | Qué hacía antes | Qué hace ahora | Quién dependía de él y cómo se reconecta |
|---|---|---|---|---|---|
| E-01 | `Clases/Validaciones/ValidarRes.cs` — clase `ValidadorRes` | **Sale** | Un único `if` de cuatro condiciones (`ValidarRes.cs:19`): nulidad, nombre vacío, peso ≤ 0 y edad ≤ 0, con un solo texto de error para las cuatro | Ya no existe | `GuardadoValidado` la recibía como `IValidador<Res>`. Sigue recibiendo un `IValidador<Res>`, pero la raíz le entrega un `ValidadorCompuesto<Res>` con las cuatro reglas (E-13, E-14). Ningún llamador cambió de firma |

## Lo que entra

| ID | Elemento | Estado | Qué hacía antes | Qué hace ahora | Quién dependía de él y cómo se reconecta |
|---|---|---|---|---|---|
| E-02 | `Contratos/IFabricaVacuna.cs` | **Entra** | No existía: cada tipo de vacuna estaba escrito a mano en cuatro métodos de `FabricaVacunas` | Contrato del Factory Method. Declara `Tipo`, `TipoSoportado`, `Crear(SolicitudVacuna)` y los tres textos observables (`MensajeIndividual`, `MensajeLote`, `EtiquetaErrorIndividual`) | Nadie dependía de nada equivalente. Lo consumen `FabricaVacunas` (E-17) y `MapeadorVacuna` (E-20), cada uno con su propio registro |
| E-03 | `Fabricas/FabricaVacunaBacteriana.cs` y `FabricaVacunaViva.cs` (2 clases) | **Entra** | El cuerpo de `CrearBacteriana` y `CrearViva` dentro de `FabricaVacunas` | Creadores concretos: cada uno construye su tipo y es dueño de sus textos | Se registran en `RaizComposicion` (E-32) y llegan a `FabricaVacunas` por constructor. Un tercer tipo de vacuna es una clase más y una línea más ahí |
| E-04 | `Valores/SolicitudVacuna.cs` | **Entra** | Los datos de creación viajaban como seis parámetros sueltos, distintos por sobrecarga | Objeto único de solicitud, con `ConLote(...)` para derivar cada elemento de un lote sin repetir la lista de parámetros | Unifica la firma de `Crear` para todas las fábricas. Sin él, `IFabricaVacuna` habría necesitado una sobrecarga por tipo, que es el problema que el patrón venía a resolver |
| E-05 | `Contratos/IEfectoVenta.cs` | **Entra** | No existía: el efecto estaba escrito dentro de `vender_res` | Contrato del Strategy: `TipoSoportado` y `Aplicar(hacienda, potrero, articulo)` | Lo consume `ServicioVenta.AplicarEfecto` (`ServicioVenta.cs:100-104`), que resuelve por asignabilidad de tipo |
| E-06 | `Estrategias/EfectoVentaRetiroInventario.cs` y `EfectoVentaSinEfecto.cs` (2 clases) | **Entra** | La línea `...L_reses.Remove(res)` de `ServicioVenta.cs:46` del Reto 1, sin alternativa posible | Estrategias concretas: retirar la res del potrero (`typeof(Res)`) o no tocar el inventario (`typeof(ProductoDerivado)`) | Se registran en `RaizComposicion` (E-32) y llegan a `ServicioVenta` por constructor. Vender un artículo nuevo es una clase más y una línea más ahí |
| E-07 | `Contratos/IArticuloVendible.cs` | **Entra** | No existía: `Venta` exigía una `Res` | Declara lo único que una venta necesita del artículo: `Nombre` | Es la mitad de modelado que hace viable SC-1. La implementan `Res` (E-25) y `ProductoDerivado` (E-08) |
| E-08 | `Clases/ProductoDerivado.cs` | **Entra** | No existía | Artículo vendible que no es un animal: `TipoProducto` (Lacteo/Carne/Piel), `Nombre` y `Cantidad` | Lo construye `VentaController` (E-24) desde el formulario y lo consumen `ServicioVenta` (E-21) y `MapeadorVenta` (E-22). Es SC-1 |
| E-09 | `Contratos/IPublicadorEvento.cs` | **Entra** | No existía: los publicadores no tenían nada en común y se creaban con `new` dentro del dominio | Contrato del Observer: `Informar(ContextoAviso, IReceptorEventos)` | Permite que `Potrero`, `GestorReses` y `ServicioVacunacion` reciban una colección en vez de instanciar. Borra las 8 instanciaciones de P-03 |
| E-10 | `Valores/ContextoAviso.cs` | **Entra** | Cada publicador recibía sus propios parámetros sueltos, con firmas distintas | Contexto único y compartido: `Potrero`, `CantidadReses`, `Res`, `ContadorBacterianas`, `ContadorVivas`. Cada publicador lee solo lo suyo | Es el precio declarado de unificar la firma. Sin él no hay interfaz común posible sin una firma por publicador |
| E-11 | `Clases/Validaciones/ValidadorCompuesto.cs` | **Entra** | El `if` de cuatro condiciones de E-01 | Compuesto del Composite: recorre `IReadOnlyCollection<IValidador<T>>` y **corta en la primera regla que falla**, devolviendo el mismo literal congelado (`ValidadorCompuesto.cs:38-58`) | Se construye en `RaizComposicion.cs:91-97` y se entrega a `GuardadoValidado` como `IValidador<Res>`. Para el llamador es indistinguible de un validador simple |
| E-12 | `Clases/Validaciones/ReglasRes/` — `ReglaResNoNula`, `ReglaNombreObligatorio`, `ReglaPesoPositivo`, `ReglaEdadPositiva` (4 clases) | **Entra** | Las cuatro condiciones del `\|\|` de `ValidarRes.cs:19` | Hojas del Composite: una condición por clase, cada una `IValidador<Res>` | El orden de registro reproduce el del `\|\|` original: `ReglaResNoNula` va primera porque las otras tres asumen que la res no es nula. Cambiar el orden cambia salida observable |
| E-13 | `Views/Venta/VenderProducto.cshtml` | **Entra** | No existía | Pantalla de venta de producto derivado (SC-1) | La sirve `VentaController` (E-24). Es la única vista nueva de todo el reto |

## Lo que se transforma

| ID | Elemento | Estado | Qué hacía antes | Qué hace ahora | Quién dependía de él y cómo se reconecta |
|---|---|---|---|---|---|
| E-14 | `Servicios/FabricaVacunas.cs` | **Se transforma** | Cuatro métodos fijos —`CrearBacteriana`, `CrearViva`, `CrearLoteBacteriano`, `CrearLoteVivo`— con la validación repetida en cada uno | Recibe `IEnumerable<IFabricaVacuna>` y resuelve por discriminador en `ObtenerFabrica` (`FabricaVacunas.cs:116-117`), con fábrica por defecto. El archivo **encogió** de 10 973 a 8 321 bytes | `VacunaService` (E-15) llamaba a un método por tipo; ahora llama a `Crear(tipoVacuna, ...)`. La raíz le inyecta las dos fábricas |
| E-15 | `Servicios/VacunaService.cs` | **Se transforma** | Un `if/else` sobre los dos parámetros opcionales decidía qué método de la fábrica llamar (`VacunaService.cs:49-58` del Reto 1) | Recibe `tipoVacuna` explícito y delega (`VacunaService.cs:48,61`). El `if` que quedó solo comprueba que la combinación de parámetros sea coherente, no qué tipo crear | `VacunaController` (E-16) le pasa ahora el tipo, que ya venía del formulario y antes se descartaba |
| E-16 | `Controllers/VacunaController.cs` | **Se transforma** | Un `if (tipoVacuna == "Bacteriana") … else …` elegía la llamada al servicio (`VacunaController.cs:95-107` del Reto 1) | Valida los datos del formulario y pasa `tipoVacuna` sin ramificar por él | El servicio absorbió la decisión. La vista no cambió: el `select` ya enviaba el tipo |
| E-17 | `Infraestructura/MapeadorVacuna.cs` | **Se transforma** | Cuatro ramas construían la vacuna al leer el `.txt` | Diccionario estático `FabricasPorTipo` (`MapeadorVacuna.cs:50`) y `fabrica.Crear(solicitud)` (`:114`, `:178`) | Mantiene su **propio** registro y no el de la raíz: la persistencia se instancia fuera del contenedor. Es duplicación consciente, declarada en la ficha del patrón |
| E-18 | `Servicios/ServicioVacunacion.cs` | **Se transforma** | Creaba con `new` sus publicadores y contaba bacterianas y vivas con dos contadores escritos a mano | Recibe `PublisherVacunaVencida` y `IEnumerable<IPublicadorEvento>` por constructor (`ServicioVacunacion.cs:43`) e itera la colección (`:131`) | La raíz le construye la lista **a mano**, no la resuelve el contenedor: su lista no es la misma que la de `GestorReses` y el orden es salida observable |
| E-19 | `Eventos/PublisherPesoMin.cs`, `PublisherPesoVenta.cs`, `PublisherPotreroLleno.cs`, `PublisherPotreroMitad.cs`, `PublisherVacunacionCompletada.cs` (5 clases) | **Se transforma** | Cada uno con su propio método y su propia firma: `Informar_Peso_Min(Res, IReceptorEventos)`, etc. Ninguna interfaz común | Los cinco implementan `IPublicadorEvento.Informar(ContextoAviso, IReceptorEventos)` y leen del contexto solo lo suyo | Quien los disparaba con `new` y llamada directa ahora itera una colección. **`PublisherVacunaVencida` no está en esta fila y no se modificó**: devuelve `bool` y `ServicioVacunacion` lanza según ese valor, así que es guarda de flujo, no aviso (B-07) |
| E-20 | `Clases/Potrero.cs` | **Se transforma** | Tenía cuatro campos `private Publisher… = new Publisher…()` y los disparaba en secuencia escrita a mano | Recibe `IReadOnlyCollection<IPublicadorEvento> avisos` **por parámetro** en `anadir_res` (`Potrero.cs:66`) e itera (`:108`). Ya no conoce ninguna clase de `Eventos/` | Es una entidad: no puede pedir dependencias al contenedor, así que se las pasa quien la llama. El orden de la colección reproduce el orden original mitad → lleno → peso mínimo → peso de venta |
| E-21 | `Servicios/GestorReses.cs` | **Se transforma** | Instanciaba publicadores con `new` en tres puntos | Recibe **dos** listas distintas por constructor —`avisosAltaDeRes` y `avisosAlimentacion` (`GestorReses.cs:38`)— y las itera | Necesita listas distintas para momentos distintos; por eso la raíz las construye a mano en vez de inyectar un único `IEnumerable<IPublicadorEvento>` |
| E-22 | `Infraestructura/RepositorioPotrerosArchivo.cs` | **Se transforma** | Pasaba a `Potrero.anadir_res` la política y las fábricas | Pasa además `avisosAltaDeRes` (`RepositorioPotrerosArchivo.cs`), el mismo registro que usa `GestorReses` | Al recargar, el mensaje de `anadir_res` se descarta; aun así recibe los avisos reales y no una lista vacía, para no romper la fidelidad a propósito |
| E-23 | `Servicios/ServicioVenta.cs` | **Se transforma** | `vender_res` construía la venta y borraba la res del potrero en la misma línea (`ServicioVenta.cs:46` del Reto 1). Una sola operación posible | Recibe `IEnumerable<IEfectoVenta>` (`:31`), tiene `vender_res` y `vender_producto` (`:76`), y ambos delegan en `AplicarEfecto` (`:100`), que resuelve por `TipoSoportado.IsAssignableFrom` | El comportamiento de vender una res no cambió, incluida la ausencia de guarda que ya lanzaba. `vender_producto` es SC-1 y usa **el mismo registro**, no una rama nueva |
| E-24 | `Clases/Venta.cs` | **Se transforma** | Campo `private Res res` obligatorio en el constructor | Campo `private IArticuloVendible articulo` | `ValidarVenta` (E-26), `MapeadorVenta` (E-22) y `ServicioVenta` (E-23) leían `venta.Res`; ahora leen `venta.Articulo`. Es la mitad de SC-1 que ningún patrón resuelve solo (B-10) |
| E-25 | `Clases/Res.cs` | **Se transforma** | `public abstract class Res` | `public abstract class Res : IArticuloVendible` | **No gana un solo miembro**: ya tenía `Nombre`. Vender una res sigue produciendo la misma línea de disco byte a byte |
| E-26 | `Clases/Validaciones/ValidarVenta.cs` — clase `ValidadorVenta` | **Se transforma** | `if (… \|\| venta.Res == null \|\| …)` | `if (… \|\| venta.Articulo == null \|\| …)` | Sigue siendo un validador monolítico: el Composite **no** se le aplicó. Es deuda declarada |
| E-27 | `Infraestructura/MapeadorVenta.cs` | **Se transforma** | Escribía las 7 columnas leyendo `venta.Res.Nombre`, `.Peso`, `.Edad` y el nombre del tipo | `DescomponerArticulo` (`MapeadorVenta.cs:49-58`) devuelve la misma tupla de 7 columnas para una res o para un producto | **No se abrió ninguna columna nueva** (ADR-12): un producto reutiliza las columnas de peso y tipo con su cantidad y su discriminador. Una venta de res escribe la línea idéntica a la del Reto 1 |
| E-28 | `Servicios/VentaService.cs` | **Se transforma** | Solo sabía vender reses | Gana la operación de SC-1, llamando a `ServicioVenta` y a `GuardadoValidado` | `ResService` **no se tocó**: vender un producto derivado no opera sobre ninguna res |
| E-29 | `Controllers/VentaController.cs` | **Se transforma** | Una única acción de venta | Gana la acción de venta de producto derivado y sirve la vista nueva (E-13) | Construye el `ProductoDerivado` (E-08) desde el formulario |
| E-30 | `Views/Venta/Index.cshtml` | **Se transforma** | Listaba ventas de reses | Distingue reses de productos en la misma tabla | Lee la venta a través del artículo, no de la res |
| E-31 | `Servicios/GuardadoValidado.cs` | **Se transforma** | Cinco métodos `Guardar*` repetían el mismo bucle validar → cortar → persistir (P-05) | El bucle se unificó en el genérico `Validar<T>` (`GuardadoValidado.cs:88`); cuatro de los cinco métodos quedaron en cuatro líneas | Conserva sus **7 dependencias** por constructor, y `GuardarVacunasAplicadas` conserva su bucle anidado propio porque el orden de recorrido determina cuál es el último resultado registrado, que es salida observable |
| E-32 | `Composicion/RaizComposicion.cs` | **Se transforma** | Registraba repositorios, validadores y servicios | Además registra las dos fábricas de vacuna, las dos estrategias de venta, los cinco publicadores en el orden de disparo y compone `ValidadorCompuesto<Res>` con sus cuatro reglas (`:91-97`). Creció de 9 642 a 15 398 bytes | Es **el único archivo donde se elige una implementación** en los cuatro patrones. La propiedad que sostiene el reto: un tipo nuevo dentro de cualquiera de las cuatro familias cuesta una clase y una línea aquí, y ningún condicional nuevo en `ServicioVenta`, `Potrero`, `GestorReses`, `ServicioVacunacion` ni `GuardadoValidado` |

## Cómo cuadra el conteo

| Estado | Clases e interfaces | Vistas | Total |
|---|---|---|---|
| Sale | 1 | 0 | 1 |
| Entra | 16 | 1 | 17 |
| Se transforma | 22 | 1 | 23 |

Las 16 clases que entran son las 4 del Factory Method (E-02…E-04), las 5 del Strategy y SC-1
(E-05…E-08), las 2 del Observer (E-09, E-10) y las 5 del Composite (E-11, E-12).
